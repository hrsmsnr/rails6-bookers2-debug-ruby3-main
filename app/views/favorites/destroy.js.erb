<%
=begin
%>
$('#favorite-btn_<%= @book.id %>').html("<%= j(render "favorites/favorite-btn", book: @book) %>");
はidセレクター
$(".favorite-btn_<%= @book.id %>").html("<%= j(render 'favorites/favorite-btn', book: @book) %>");
はクラスセレクター
<%
=end
%>

$(".favorite-btn_<%= @book.id %>").html("<%= j(render 'favorites/favorite-btn', book: @book) %>");